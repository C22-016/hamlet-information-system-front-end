import React from 'react';
import Layout from './Layout';
import FormUpdateMessage from '../components/Messages/FormUpdateMessage';

const UpdateMessage = () => {
  return (
    <Layout>
      <FormUpdateMessage />
    </Layout>
  );
};

export default UpdateMessage;
