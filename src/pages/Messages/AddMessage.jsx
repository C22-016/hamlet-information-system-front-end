import React from 'react';
import Layout from '../Layout';
import AddNewMessage from '../../components/Forms/FormAdd/AddNewMessage';

const AddMessage = () => {
  return (
    <Layout>
        <AddNewMessage />
    </Layout>
  );
};

export default AddMessage;
